node_modules/
bin/
.DS_Store
*.log
.env
.env.*
!.env.*.example
scripts/launchd/*.plist
!scripts/launchd/*.plist.template
# Bun --compile temp artifacts. Each build emits a new hash-named .bun-build
# file in cwd; glob catches all of them.
*.bun-build
.gstack/
supabase/.temp/
.claude/skills/
.idea
# Upstream
eval/reports/
eval/data/world-v1/world.html

# BrainBench amara-life-v1 Opus cache (regenerate via eval:generate-amara-life)
eval/data/amara-life-v1/_cache/

# Claude Code / OMC runtime state — per-session, machine-local, not source
.omc/

# Launchd log outputs (kos-patrol / enrich-sweep / notion-poller)
skills/kos-jarvis/kos-patrol/patrol.std*.log
skills/kos-jarvis/enrich-sweep/sweep.std*.log
workers/notion-poller/poller.std*.log
