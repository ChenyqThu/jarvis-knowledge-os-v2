{
	"name": "kos-worker",
	"version": "0.2.0",
	"private": true,
	"description": "Notion Worker for Jarvis Knowledge OS v2 — query, ingest (url or markdown), digest, status",
	"scripts": {
		"build": "tsc",
		"check": "tsc --noEmit"
	},
	"engines": {
		"node": ">=22.0.0",
		"npm": ">=10.9.2"
	},
	"devDependencies": {
		"@types/node": "^22.9.0",
		"tsx": "^4.20.6",
		"typescript": "^5.8.0"
	},
	"dependencies": {
		"@notionhq/workers": ">=0.0.0"
	}
}
